[user]
  name = Santos Gallegos
  email = stsewd@proton.me
  signingkey = C1AD42C27116CB5A

[init]
  defaultBranch = main

[push]
  default = current
  autoSetupRemote = true

[core]
  excludesfile = ~/.gitignore
  editor = nvim
  pager = delta

[interactive]
  diffFilter = delta --color-only --features=interactive

[delta]
  line-numbers = true

[delta "light"]
  light = true

[diff]
  # Uses different colors to highlight lines in diffs that have been “moved”.
  colorMoved = default
  algorithm = histogram

[pull]
  ff = only

[merge]
  # Show three parts of the diff when there is a conflict.
  conflictStyle = diff3

[commit]
  gpgsign = true

[hub]
  protocol = ssh
